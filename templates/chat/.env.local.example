# LifeRX Brain Environment Variables
# Copy this file to .env.local and fill in your values

# =============================================================================
# SUPABASE (Required)
# =============================================================================
# Your Supabase project URL (Settings > API)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# Supabase anonymous key (Settings > API > anon/public)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Supabase service role key (Settings > API > service_role/secret)
# WARNING: Keep this secret! Only used server-side.
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# =============================================================================
# OPENAI (Required)
# =============================================================================
# Your OpenAI API key
OPENAI_API_KEY=sk-your-openai-key

# Model to use for chat completions (optional, default: gpt-4o)
# OPENAI_MODEL=gpt-4o

# =============================================================================
# INTERNAL API (Required)
# =============================================================================
# Shared secret for internal API authentication
# Generate a secure random string (e.g., openssl rand -base64 32)
INTERNAL_SHARED_SECRET=your-internal-secret

# Default organization ID for single-tenant setups
DEFAULT_ORG_ID=your-org-uuid

# =============================================================================
# APP CONFIG (Optional)
# =============================================================================
# Public URL of the app (for callbacks, etc.)
# NEXT_PUBLIC_APP_URL=http://localhost:3000

# =============================================================================
# FIRECRAWL (Optional - for web scraping)
# =============================================================================
# Firecrawl API key (get one at https://firecrawl.dev)
# FIRECRAWL_API_KEY=fc-your-firecrawl-key
